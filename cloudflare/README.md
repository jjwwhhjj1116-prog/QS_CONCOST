# Cloudflare 무료 이전 사이트
브랜치: `codex/cloudflare-migration`. 도메인/DNS와 main 소스는 아직 전환하지 않았다.

## 2026-09-08 자동메일 전환 (현재 설정)
- 사용자 요청에 따라 Cloudflare 자체 Cron을 활성화: 월~금 KST 09:00 수집 시작(09:55 기한), 10:00 메일.
- `MAIL_MODE=live`, `SCHEDULE_ENABLED=true`. 관리자 세션 없이 실행한다.
- 기존 Render 주소로 보내는 GitHub workflow 307850329는 disabled_manually.
- Render 사이트 자동발송 중지 및 내부 `SCHEDULE_JOBS=0` 저장 적용. 키/주소록은 삭제하지 않음.
- Render의 승인된 3개 환경변수를 Worker Secret으로 복사했으며 수신자 7명을 D1에 한 번만 가져왔다.
- 전용 `concost-mail-dispatch` Queue 사용: 수집 Queue 정체와 메일을 분리.
- 당일 원문 게시일이 확인된 자료만 메일에 포함. 오늘 자료가 없으면 `no_verified_today_items`를 남기고 과거 자료를 재발송하지 않음.
- 날짜/수신자 DB 유일키 + Resend idempotency key; 재전달 시 접수 완료 건은 재발송하지 않음.
- `/api/automation/status`는 주소/키를 제외한 예약·발송 상태 확인용. 접수 성공은 받은편지함 도착과 다르다.
- Node 26개 검증 통과(로그인 없는 Cron, 주소록 영속성, 중복 발송, 지연/빈 자료 차단 포함). 실제 수신 검증은 아직 안 함.
- 2026-09-09 10:00 KST Codex 확인 예약 ID `10-concost` 생성. 실패 시 자동 재발송하지 않음.
- 배포 버전: `88371bfd-1f64-4f44-ad57-918cb266c20b`.
- 아직 미완료: Cloudflare 관리자 비밀번호 설정, 일부 원기관 TLS/HTTP400/시간초과, 전체 자료 포괄성·무료 CPU 검증.
- 이후 추가된 뉴스/법령/직접기관/LH/K-water/K-apt/도로공사/사업정보 수집기는 부분 성공 상태이며 모든 기관 성공으로 보지 않는다.

## 아래는 자동메일 전환 전 최초 시험 기록 (현재 설정이 아님)

시험 주소: https://concost-migration-trial.jjwwhhjj1116.workers.dev

## 현재 단계
**무료 Worker + D1 + Queue 배포 및 공공데이터 API 실수집 1회 검증 완료. 전체 기능 이전은 미완료.**
유료 Containers/SDK/Docker 의존성을 배포 구성에서 제거했다.
실제 메일 발송 코드/키는 없고, 크론도 비활성화했다. 이 시험판을 운영으로 전환하면 안 된다.

## 구조
동일한 웹 화면 → Worker 조회 API → D1
수집 요청 → Queue → Worker가 나라장터·누리장터 API 한 페이지 조회 → D1 저장 → 다음 페이지 Queue

- 5개 API 분야(나라장터 공사/용역, 누리장터 공사/용역/기타), 날짜별 분리.
- 한 번에 20건. 무료 CPU 제한을 고려한 크기이며 실제 CPU 사용량 검증은 아직 필요.
- 이전 Python 적합도 사전은 JSON으로 공유하며 JS 판정과 점수·사유 일치 테스트를 수행.
- 40점 이상 + 안전진단 서울 제한을 유지. 기존 지역 판별의 동명 자치구 문제는 별도 검수 대상.
- 수동 수집 5분 기한. API 호출 15초 제한. 수집 페이지마다 데이터와 다음 페이지 번호를 원자적으로 저장.
- Queue 중복 전달/일시 실패는 페이지 체크포인트와 짧은 임대로 처리.
- API 오류, 빈 응답, 페이지 누락, 필터 제외, 기한 초과를 구분.
- 일일 시험 수집 2회 / API 페이지 1,000회 상한. 계정 전체 무료 한도는 다른 앱과 공유됨.
- 상한/기한에 걸리면 부분 결과를 보존하고 실패/만료로 표시하며 완전 수집으로 주장하지 않는다.
- 운영 메일은 보내지 않음. `/api/trial/digest`는 게시일이 오늘인 데이터의 JSON 미리보기만 반환.

## 미완료 범위
지원COK/뉴스/법규/LH/K-apt/도로공사/K-water/사전 사업정보/공사비 분석의 Workers 수집기는 아직 미이전.
관리자 로그인/주소록/설정 저장/관심 상태/실제 메일/운영 스케줄도 미이전.
기존 화면을 재사용하므로 탭이 남아 있지만 **미연결 탭의 0은 수집 결과가 아니다**.
상단 시험 안내와 API 미설정 오류로 이를 명시한다.

## 검증 (2026-09-08)
- Python 회귀·교차 런타임 검증 93개 통과.
- Node 날짜/응답/페이지/적합도 검증 11개 통과.
- 무료 Worker 빌드 및 workers.dev 실제 배포 성공.
- 신규 무료 trial D1 스키마 적용 성공; trial Queue 2개 생성.
- 사용자 승인 후 공공데이터 API 키를 시험 Worker Secret에 저장. 운영 키 저장소는 변경하지 않음.
- 키 저장 직후 인증 반영 지연으로 첫 요청은 401; 수집 작업이 생성되지 않았음을 D1에서 확인.
  시험 스크립트는 읽기 전용 인증 확인을 최대 60초 수행한 뒤 수집 POST를 한 번만 전송하도록 보완.
- 실제 수집 run `8d4aefbc-b488-4515-8724-b32a0f4ef36d`: 최근 168시간, 35개 작업 모두 succeeded,
  197.3초, 285페이지, 후보 5,284건, 적합 저장 11건(나라장터 7/누리장터 4), 오류 0.
  분야별 후보 수는 API reported total과 일치: 나라장터 공사 2,084/용역 2,915,
  누리장터 공사 51/용역 234/기타 0. 이 수치는 해당 1회 API 조회 범위 기준이며 모든 공고의 포괄성을 뜻하지 않음.
- D1 저장 건수, 공개 조회 API, 실제 브라우저의 QS 입찰공고 11건 표시를 대조.
  취소공고 1건을 포함한 저장 레코드 수이며 신규 기회 11개라는 뜻은 아님.
- 시험 메일 미리보기 sent=false/mode=dry-run 확인. 실제 메일 미발송.
- 기존 유료 시제품의 로컬 Python 실행에서 나라장터 8건/누리장터 4건을 확인한 이력은 있으나,
  **이 결과는 무료 Worker 실환경 수집 결과가 아니다**.
- 실환경 API/Queue 경로는 위 1회 통과. CPU 사용량 계측, 중복 전달/강제 중단 장애 주입,
  원문 상세 링크 대조와 반복 운영 안정성 검증은 아직 남아 있음.
- 결제/플랜 업그레이드/운영 도메인 전환/메일 발송은 하지 않았다.

## 키 이전 범위
사용자가 회사 Cloudflare 시험 서버로 API 키 이전을 승인했다.
현재 이전 완료는 `DATA_GO_KR_SERVICE_KEY`(나라장터/누리장터 공용)이며,
시험 전용 무작위 관리자 토큰도 별도로 저장했다. 실제 관리자 비밀번호는 코드/문서에 기록하지 않는다.
법령/메일 등 나머지 키는 해당 기능 이전 단계에서 연결하며, 이번 실행에서는 복사하지 않았다.
운영 도메인 전환과 실제 시험 메일 발송은 별도 확인 없이 하지 않는다.

재검증이 필요할 때 repo root에서 (일일 시험 수집 2회 제한):
```powershell
python -X utf8 cloudflare/live_check.py https://concost-migration-trial.jjwwhhjj1116.workers.dev --install-trial-secrets
```
이 명령은 API 키를 시험 Worker Secret으로 저장하고 시험 관리자 토큰을 회전시킨 뒤 실제 수집을 실행한다.
키는 CLI stdin/인증 헤더로만 전달하며 파일/출력에 남기지 않는다. 메일 키는 복사하지 않는다.

## 재현 명령
```powershell
python -m unittest discover -s tests -q
cd cloudflare
npm ci --ignore-scripts
npm test
npx wrangler deploy --dry-run
npx wrangler d1 migrations apply concost-migration-trial --local
npx wrangler dev --local
```
`tender_radar/isolated_collector.py`는 이전 Python 결과를 대조하는 로컬 진단용으로만 남겼다.
무료 Worker에서는 실행되지 않는다. Docker 설치나 Workers Paid가 필요하지 않다.

## 운영 전환 합격 조건
1. 실제 API의 전체 페이지 수/후보 수/선별 수를 원문과 대조하고 CPU/Queue/D1 제한 확인.
2. 중복 전달·재시작·시간초과 중 부분 데이터 보존과 화면 표시 확인.
3. 나머지 기관 및 관리자/주소록/설정/메일 기능 순서대로 이전.
4. 영구 발송 이력/날짜별 원자적 잠금/Resend idempotency 검증 후 승인된 시험 수신자에게만 발송.
5. 사용자 승인 뒤에만 운영 예약을 한 곳으로 통합하고 도메인 전환.

예약은 목표 시각이며 외부 지연과 수신함 도착 시각까지 정각임을 보장하지 않는다.

## 인수인계: 오늘 요약 부족 진단 (2026-09-15)

- 확인 브랜치 `codex/cloudflare-migration`, 코드 HEAD `8632fb7`, 시작 시 미커밋 변경 없음. 아래는 앞선 9월 8일 시험 기록과 구분한 최신 읽기 전용 진단이다.
- 12:58 KST 공개 운영 API 확인: 마지막 수집 기록 12:03:31, 누적 공고 26건 / 뉴스·법령 116건. 등록일이 9월 15일인 저장 자료는 공고 0건 / 건설뉴스 1건 / 법령 8건. 이 수치는 원기관 전체의 오늘 공고 수가 아니다.
- 자동메일 상태: 9월 15일 10:00:37 스케줄 실행, 수신자 7명 모두 provider_accepted. 개별 메일 본문과 사용자 수신함은 이번에 확인하지 않았다.
- `buildPreview`는 발송 이력에 있는 당일 뉴스도 제외한다. 동일 뉴스 fixture로 발송 전 1건 → 발송 이력 전달 후 0건 재현. 따라서 오늘 요약 UI가 당일 전체가 아니라 미발송 자료로 줄어드는 코드 경로가 확인됐다. 실제 관리자 화면의 정확한 제외 목록은 미검증.
- 최근 source_health: CERIK 2개·RICON 2개 HTTP 403, 국토부 시간초과, LH·서울교통공사 HTTP 526, 공동주택 API HTTP 400. 낙찰 공사·계약 공사·발주계획 용역·낙찰 용역·계약 용역은 `trial_daily_page_limit`. 이는 코드의 일일 1,000페이지 보호 한도이며 Cloudflare 플랫폼 오류로 단정하지 않는다. source_health는 소스별 최근 작업 기록이므로 여러 날짜 작업의 합계로 해석하지 않는다.
- 코드/설정/배포/운영 데이터 변경 및 재발송 없음. 이 인수인계만 추가했다.
- 다음 작업: 조회용 오늘 요약과 발송용 미발송 선별을 분리하는 최소 수정 검토 → 입찰/뉴스 수집 예산과 대량 계약 이력 수집 예산 분리 → 실패 기관의 정상 지원 경로 점검 → 조회 시간대/전체 페이지/적합도 탈락 원문 대조. 운영 배포는 승인 범위를 확인한 후 진행한다.
- 13:00 KST 예약메일 추가 확인: 저장된 발송 날짜는 9/9(보완), 9/10, 9/15이며 각 7명 provider_accepted. 평일 9/11·9/14는 날짜별 발송 준비/접수 기록 모두 없음. `days` LIMIT 7에도 전체 3개만 반환돼 최근 목록 제한 때문에 빠진 것으로 보이지 않는다. 주말 9/12·9/13은 원래 대상 아님. Resend의 실제 배달/반송 상태는 미확인이다.
- 코드상 `no_verified_today_items`는 delivery_days 생성 전에 중단되며, scheduler 오류는 마지막 값 하나로 덮어쓴다. 예약 실행도 10:00분만 허용하고 대기열 발송은 10:04분까지 제한한다. 따라서 발송 생략/지연 취약 경로는 확인했지만 9/11·9/14의 개별 원인은 과거 실행 로그 없이는 확정 불가. 다음 조치에 날짜별 예약 결과(실행/생략 사유/실패) 보존과 미발송 경고를 포함해야 한다. 늦은 재발송이나 빈 내용 메일로 정책을 임의 변경하지 않았다.

공식 무료 한도:
- https://developers.cloudflare.com/workers/platform/limits/
- https://developers.cloudflare.com/d1/platform/pricing/
- https://developers.cloudflare.com/queues/platform/pricing/

## 수정 및 배포 인수인계 (2026-09-15, 예약메일 누락)

- 위 진단 후 사용자 수정 요청으로 기존 무료 Worker에 최소 수정 반영. API 키·주소록·DB 스키마·Render·기존 발송 기록은 변경하지 않음.
- 10:00 최초 예약 유지, UTC `0-4 1 * * MON-FRI`로 10:01~10:04 복구 시도. 당일/수신자 잠금과 Resend idempotency를 유지해 이미 접수된 메일은 재발송하지 않는다. 10:05 이후 및 전날 지연 이벤트 차단. 정확한 수신함 도착 시각까지 보장하는 변경은 아니다.
- 당일 신규 자료가 없으면 예약 자체를 중단하는 대신 `수집 점검 안내 · 당일 신규 자료 미확인`을 한 번 발송. 어제 자료를 오늘 자료로 바꾸거나 실제 공고 0건으로 확정하지 않는다. 본문에 최근 수집 작업의 완료/진행/실패 상태를 표시.
- 관리자 오늘 요약은 당일 발송 완료 자료도 유지. 실제 발송 선별은 기존 미발송 필터 유지. 과거 뉴스 재사용 없음.
- 날짜별 예약 시도 결과는 기존 settings의 `scheduler:digest:` 전용 키로 기록; `/api/automation/status`의 attempts에 최근 35개 표시. 별도 DB 변경 없이 기존 마지막 오류 덮어쓰기의 한계를 보완. 과거 누락 원인을 소급 생성하지 않음.
- 검증: Node 42/42 통과, `wrangler deploy --dry-run` 성공, `git diff --check` 성공. 큐 등록 실패 후 10:01 복구, 수신자 중복 요청, 다음 날 지연 요청 차단, 신규 자료 없는 안내, 조회/발송 필터 분리 검증. 테스트는 fixture 수신자와 mock provider로 실행했고 실제 재발송하지 않음.
- 원격 branch HEAD가 로컬 시작 HEAD `8632fb7`과 같은 것을 확인 후 배포. Worker 버전 `5a95e05b-62e8-4a49-a38a-edc3fc1398f1`; 기존 URL 유지. 배포 출력에서 09:00 수집 및 10:00~10:04 발송 복구 예약 확인.
- 남음: 다음 평일 실제 예약 실행/Resend 배달 결과 확인(아직 미검증), 실제 관리자 브라우저 재로그인 후 오늘 요약 확인, 09:55까지 반복 수집 및 일일 수집 예산 분리, 각 기관 403/526/400 오류 복구. 이 수정은 해당 크롤러 오류까지 해결했다고 주장하지 않는다.
- 다음 시작: 현재 브랜치/원격 커밋 확인 → automation/status attempts·days·counts 대조 → 다음 날 실제 접수/배달 확인 → 크롤러 수집 누락 작업 진행. 이전 버전으로 되돌리면 발송 생략과 요약 필터 문제도 다시 생기므로 복구 전 영향 확인.
